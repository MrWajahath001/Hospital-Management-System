spring.application.name=backend
server.port=${PORT:8080}

# ============================================
# Database Configuration (TiDB Cloud / MySQL)
# ============================================
spring.datasource.url=${DATABASE_URL:jdbc:mysql://${HOST:gateway01.ap-northeast-1.prod.aws.tidbcloud.com}:4000/${DATABASE:hospital_db}?useSSL=true&requireSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DATABASE_USERNAME:${DB_USER:${USERNAME:23piNfQFUzKUJWV.root}}}
spring.datasource.password=${DATABASE_PASSWORD:${DB_PASSWORD:${PASSWORD:t928mFKzq9KJmuF2}}}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# ============================================
# Security / JWT Configuration
# ============================================
app.jwt.secret=${JWT_SECRET:HospitalManagementSystemSecretKey2026!!}
app.jwt.expiration=86400000

# ============================================
# CORS Configuration (allows frontend)
# ============================================
app.cors.allowed-origins=${FRONTEND_URL:*}
